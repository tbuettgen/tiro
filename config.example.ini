# Tiro configuration (reference).
#
# Tiro creates its own config.ini from built-in defaults on first run and
# rewrites it whenever you change a setting in the panel (Ctrl+Alt+C), so you
# normally never edit this by hand. Copy this file to config.ini only if you
# want to pre-seed your settings.
#
# Two things to know:
#   * Tiro strips comments the next time it saves the file.
#   * Do NOT put trailing/inline comments on a value line — everything after
#     "key =" is taken literally as the value. Use full-line comments only.

[general]

# Hotkeys (modifiers + key joined with "+", e.g. ctrl+alt+space).
# Registered as system-wide global shortcuts; if a combo is already in use
# by another app it can't be claimed (rebind it in Settings -> Shortcuts).
dictation_hotkey = ctrl+alt+space
paste_hotkey = ctrl+alt+v
panel_hotkey = ctrl+alt+c
cancel_hotkey = ctrl+alt+x

# Transcription engine.
# device: auto adapts to the machine — on a discrete-GPU battery laptop it
# uses the GPU when plugged in and the CPU on battery; on an integrated GPU
# it stays on the GPU and only swaps models; on a desktop (or with
# treat_as_desktop below) it always prefers the GPU. Or force cpu | gpu
# (the legacy value "cuda" is accepted and treated as gpu).
device = auto
# Ignore battery state: a laptop that lives docked can opt into desktop
# behavior (single model, no battery/AC switching).
treat_as_desktop = false
# Multi-GPU machines only: the Vulkan device the GPU engine runs on, as
# reported by `tiro --gpu-enum` (index + name; the name guards against
# hardware changes — if it no longer matches, Tiro falls back to the best
# device). Leave both empty to auto-pick (discrete first, then most VRAM).
gpu_device_index =
gpu_device_name =
# CPU model quantization (the GPU path always runs full precision).
compute_type = int8
# Lighter / faster model used on CPU / on battery.
model_battery = base.en
# More accurate model used on GPU / when plugged in.
model_ac = small.en
# Model used while device is forced to cpu or gpu (the single "Model" row
# in Settings -> Engine); also the legacy fallback if the two models above
# are missing.
model = base.en

# Microphone: matched as a case-insensitive substring of the device name.
# Leave blank to use the first available microphone.
mic_name =

# Clipboard cleanup applied to the copied text: none | light | fillers.
clipboard_cleanup = light
# Prime Whisper with vocab.txt so it spells your names / jargon correctly.
use_vocab_bias = true
# Show the floating "Recording" pill while dictating.
pill = true
# Where the pill docks on the active monitor: top | bottom, and its distance
# in px from that edge of the work area (clamped to half the screen height).
pill_position = bottom
pill_padding = 110
# Soft sound cues, and their loudness (0.0 - 1.5).
beeps = true
sound_volume = 1.0

# Appearance: theme is system | light | dark; transparency is 0 (solid) - 100
# (default 45; 70 on macOS, where the panel sits on a native glass backdrop).
theme = system
panel_transparency = 45
# macOS only: blur the desktop behind the panel (Liquid Glass on macOS 26,
# the system HUD material before that). Ignored elsewhere.
liquid_glass = true

# The first-run setup guide shows until it is finished or skipped. Set this
# back to false to see it again, or open it from the menu bar / tray menu.
setup_done = false

# Where verbatim transcripts are written (JSONL + Markdown, one file per day).
# Defaults to Documents/Tiro in your home folder; change it in
# Settings -> Storage. If this path is unwritable, Tiro falls back to the
# "logs" folder next to the app.
vault_dir = /home/yourname/Documents/Tiro
fallback_dir = logs
